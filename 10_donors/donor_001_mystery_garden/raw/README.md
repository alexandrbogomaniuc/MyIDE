# Raw Donor Evidence

- This folder is for immutable, normally delivered client-side donor materials only.
- Files stored here are evidence, not editable source.
- Do not modify files in place.
- If a file needs normalization or extraction, create a derived artifact in `../evidence/` and reference the raw item ID.
