# Internal

This folder is the clean source-of-truth area for reconstructed project data.

## Rules
- Internal data must not depend on donor raw files at runtime.
- Editable project state belongs here, not in donor folders.
