# Imports

This folder holds import manifests and notes that bridge donor findings into clean internal data.

## Rules
- Imports must write internal project data, not donor raw material.
- Unknowns should stay explicit instead of being guessed silently.
