# Projects

- Clean internal MyIDE projects live here.
- `registry.json` is the workspace-facing list of projects that the shell reads.
- Each project has a `project.meta.json` file with donor linkage, target/resulting-game metadata, phase/status, verification status, key paths, and proven vs planned notes.
- Do not store raw donor evidence in this folder.
