# VABS Contract Notes

These notes define the minimum archived round contract needed before a real project-specific GS VABS renderer is built.

## Scope
- archived row payload shape
- `betData` keys
- `servletData` keys
- deterministic replay expectations
- `ROUND_ID` requirement

## Rule
Do not treat these docs as production-complete until they have been checked against real target rows for the game being shipped.
