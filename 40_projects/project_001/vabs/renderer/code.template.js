/*
 * GS VABS renderer template for project_001.
 * Template only: replace with a project-specific renderer after the archived row
 * contract, folder mapping, and deployment path are locked.
 */

function start() {
  console.log("project_001 VABS renderer template loaded");
  console.log("Replace renderer/code.template.js with the real project-local code.js later.");
}

function createRowEvent(row) {
  return row;
}

function draw(row) {
  return row;
}
