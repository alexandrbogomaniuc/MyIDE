# Internal

## Purpose
- Internal clean model roots for `project_001`.
- Transitional placeholder while `project.json` remains the active internal replay source.

## Notes
- Keep replay/runtime data internal-only.
- Do not point internal data back to donor raw files.
